import PagePlaceholder from '@/components/page-placeholder';

export default function CustomersPage() {
  return (
    <PagePlaceholder
      title="Customers & Visits"
      description="Track customer information, visit history, and appointment scheduling."
      icon="👥"
    />
  );
}