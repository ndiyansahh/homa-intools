import PagePlaceholder from '@/components/page-placeholder';

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Configure system settings, user permissions, and application preferences."
      icon="⚙️"
    />
  );
}